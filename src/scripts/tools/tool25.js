// Json viewer

class JsonViewer {
    constructor() {
        // Implementiere hier die Logik für Json viewer
    }

    init = () => {
        // Implementiere hier die Logik für Json viewer
    }

    start = () => {
        // Implementiere hier die Logik für Json viewer
    }
}

let tool25 = new JsonViewer();